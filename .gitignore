node_modules/
dist/
build/
# Compiled Neo contract artifacts are inputs, not build output: the
# production-safety tests read them to pin the client against the ABI.
!contracts/build/
coverage/
.turbo/
*.tsbuildinfo

# .NET
bin/
obj/

# env / secrets
.env
.env.*
!.env.example

# tooling noise
.DS_Store
*.log
test-results/
playwright-report/

# Local knowledge-graph output; never committed.
graphify-out/
